// using System.Security.Principal;
// using System;
// using System.Collections.Generic;

// class Program
// {
//     static void Main(string[] args)
//     {
        

        // while(true)  {
        //     Token token = nuevoScanner.TakeToken();
        //     if(token.Type == TypeToken.End)
        //         break;
        //     Console.WriteLine(token.Type + " <" + token.Row + "," + token.Col + ">");
        // }

        /*List<Token> lista = [];

        while (true)
        {
            lista.Add(nuevoScanner.TakeToken());
            if(lista[lista.Count - 1] is TokenError)  {
                Console.WriteLine(lista[lista.Count - 1].Type + " " + ((TokenError)lista[lista.Count - 1]).Mistake);
            }
            else
                Console.WriteLine(lista[lista.Count - 1].Type);


            if (lista[lista.Count - 1].Type == TypeToken.End)
                break;
        }*/



//     }
// }


